# Voice Input Bridge 🎙️

> **手机语音秒变电脑打字**：电脑没有麦克风也能用！把手机变成你的 Linux 专属无线语音输入法。  
> 利用手机端极高精度的输入法（豆包、微信输入法、讯飞、Gboard 等），通过局域网秒级上屏到 Linux 当前光标输入框与剪贴板。

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python: 3.8+](https://img.shields.io/badge/Python-3.8+-green.svg)]()
[![Platform: Linux Wayland](https://img.shields.io/badge/Platform-Linux%20Wayland-orange.svg)]()

---

## 🎯 它能解决你的什么需求？

如果你在使用 Linux 时遇到了以下情况，这个项目就是为你准备的：

- [x] **电脑没有外置麦克风**：台式机或特定工作站没有麦克风，不想为了偶尔语音打字额外花钱买硬件。
- [x] **Linux 缺少好用的系统级语音输入法**：目前 Linux 桌面很难找到开箱即用、识别精度高的系统级中文语音输入工具。
- [x] **电脑跑语音大模型太重太卡**：在电脑本地折腾 Whisper、FunASR 等模型既吃显存、占 CPU、风扇狂转，识别中英混杂效果还不理想。
- [x] **手机输入法语音极好用，却无法在电脑上用**：手机上的豆包输入法、微信输入法、讯飞识别率极高，能自动纠错润色，但无法直接把打出的字送进 Linux。

### 💡 核心解决思路

**让专业设备做专业的事，中间极简互联**：
- **手机端**：只负责最擅长的“语音转文字”（直接利用手机自带的成熟语音输入法，**手机端 0 安装 App，扫码即用**）；
- **电脑端**：只负责“接收文本并毫秒级上屏”（单文件轻量脚本，**不吃一分钱电脑算力**）。

---

## 🚀 怎么使用？

### 第一步：电脑启动（只需 1 条命令）

通过终端克隆并运行交互式控制中心：

```bash
git clone https://github.com/yyxxd/voice-input.git
cd voice-input
./menu.sh
```

> **🖥️ 偏好图形界面？**
> 在文件管理器中直接双击 **`voice-input.desktop`**（或双击 `menu.sh`），会自动弹开终端控制中心窗口！

首次运行时，脚本会**自动静默自检依赖**（若缺少工具会自动提示并一键安装，总大小 < 200 KB）。  
终端随后将展示一个清晰的动态仪表盘与手机连接二维码：

```text
┌─────────────────────────────────────────────────────────────┐
│ 🎙️  Voice Input Bridge 控制中心                           │
├─────────────────────────────────────────────────────────────┤
  ● 运行状态: 运行中 (端口: 58002)     ● 开机自启: 已启用
  📱 手机直连: http://192.168.1.100:58002
├─────────────────────────────────────────────────────────────┤
  手机扫码直达输入页:
    [ 二维码 ]
└─────────────────────────────────────────────────────────────┘
```

---

### 第二步：手机扫码连接

1. 确保手机和电脑连接**同一 Wi-Fi**（或者同一 Tailscale / 节点小宝等虚拟组网）；
2. 手机打开系统相机、微信或浏览器“扫一扫”，扫描终端上的二维码（或直接访问显示的网址）。

---

### 第三步：日常语音打字体验

1. **光标就位**：在电脑上鼠标点击任何你想输入的地方（浏览器搜索框、聊天窗口、VS Code 或终端）。
2. **说话输入**：
   - 在手机网页的输入框内点击，唤出手机键盘；
   - **长按手机输入法的语音键说话**（可利用豆包、微信输入法等极速识别与自动润色）；
3. **一键上屏**：
   - 点击手机底部的 **「发送至电脑」**；
   - 电脑音响会发出一声轻快的“嗒”提示音，**文字瞬间出现在电脑当前光标处**！
   - *（注：如果电脑当前没有聚焦在任何输入框，文字也会**完整保存在系统剪贴板中**，按 `Ctrl+V` 即可粘贴，绝不丢失。）*
4. **无缝连续输入**：
   - 点击发送后，手机软键盘**绝不收起**，方便你紧接着长按语音键说下一句。

---

### 进阶使用：开机静默后台常驻

当你觉得好用、想每天随开随用时：
在 `./menu.sh` 菜单中输入 **`3`**，一键注册为系统服务（Systemd）。从此电脑开机自动在后台静默运行，无需打开任何黑框终端，手机随时随地连入打字。

---

## 🔬 技术实现与进阶说明

### 1. 系统要求与环境

- **系统平台**：Linux（Wayland 会话，已在 Hyprland、Sway 验证通过，兼容 GNOME Wayland / KDE Wayland）
- **Python 版本**：Python 3.8+（纯标准库，无需 `pip install` 任何第三方包）
- **轻量依赖包**：
  - `wl-clipboard`：提供 Wayland 原生剪贴板读写能力（`wl-copy` / `wl-paste`）
  - `wtype`：基于 Wayland virtual-keyboard 协议的原生按键模拟工具（用于模拟 `Ctrl+V` 粘贴）
  - `qrencode`：用于在终端打印高对比度 UTF-8 字符二维码

### 2. 双通道无损上屏策略 (Dual-Channel Strategy)

为什么不直接用 `wtype` 打字，而采用“剪贴板 + 模拟粘贴”？
- **直接击键模拟缺陷**：直接模拟输入中文容易遇到 Unicode 编码截断、打字速度缓慢以及与宿主机输入法候选词冲突的问题。
- **双通道方案优势**：
  1. **无条件写入剪贴板**：收到请求后 20ms 内通过 `wl-copy` 写入 Wayland 剪贴板，保障数据物理不丢失；
  2. **触发极速粘贴**：调用 `wtype -M ctrl -k v -m ctrl` 模拟触发粘贴，瞬间完成整句/整段中英文上屏；
  3. **非阻塞音频反馈**：异步调用 PipeWire（`pw-play`）播放轻微音效，提供盲打听觉确认。

### 3. 移动端高保真交互设计

- **底部吸附底栏（Docked Footer）**：控制按钮紧贴大拇指黄金操作区，避免手机软键盘弹起时视口遮挡。
- **软键盘焦点保活（Focus Retention）**：通过拦截 `pointerdown` 默认失焦事件，彻底解决移动端浏览器点击按钮后输入法键盘强制收起的痛点。
- **深浅双主题系统**：遵循 Apple / Linear 级 Design Tokens，支持右上角一键切换主题并持久化在本地。
- **本地历史记录**：所有记录保存在手机浏览器的 `localStorage` 中，支持统计字数、时间、单条一键重发与清空。

### 4. 命令行参数与自定义配置

直接运行后端核心脚本时支持的参数：

```bash
python3 voice_input.py [选项]
```

| 参数 | 默认值 | 说明 |
| :--- | :--- | :--- |
| `--port` | `58002` | 服务监听端口（若被占用会自动切至备用端口 `53317`） |
| `--host` | `0.0.0.0` | 监听的主机地址（`0.0.0.0` 允许局域网外部设备访问） |
| `--display-ip` | 自动探测 | 终端二维码与首选展示的 IP 地址（支持环境变量 `VOICE_DISPLAY_IP`） |
| `--sound` / `--no-sound` | 启用 | 控制电脑端文字上屏成功时的轻微清脆提示音 |

---

## ❓ 常见问题 (FAQ)

<details>
<summary><b>Q: 手机必须要和电脑在同一个 Wi-Fi 吗？不在家能用吗？</b></summary>
<b>可以！</b> 本项目原生兼容任何异地虚拟组网工具（如 Tailscale、节点小宝、蒲公英等）。只要手机和电脑处于同一虚拟局域网，手机直接访问电脑在虚拟网络中的 IP 即可无缝输入。
</details>

<details>
<summary><b>Q: 会不会记录或泄露我说的内容？</b></summary>
<b>绝对不会。</b> 整个通信全部运行在你自己的私有局域网或点对点组网内，没有第三方服务器中转。电脑端内存即用即弃，不写任何磁盘日志；历史记录仅保存在你自己的手机浏览器本地。
</details>

<details>
<summary><b>Q: 支持 X11 桌面吗？</b></summary>
目前核心版本针对现代 Linux 主流的 <b>Wayland</b> 桌面环境（基于 <code>wl-copy</code> 与 <code>wtype</code>）进行了极致优化。若在 X11 下使用，可通过替换为 <code>xclip</code> 与 <code>xdotool</code> 快速适配。
</details>

---

## 📄 开源许可证

本项目采用 [MIT License](LICENSE) 授权许可。
